<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="onedaycat.com.food.fantasy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="onedaycat.com.food.fantasy.common.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainfood.activity.MainActivity" />
        <activity
            android:name=".mainfood.activity.FoodDetailActivity"
            android:screenOrientation="portrait" />
        <activity android:name="onedaycat.com.food.fantasy.ui.order.OrderDetailActivity" />
        <activity android:name="onedaycat.com.food.fantasy.ui.signin.SignInActivity"/>
        <activity android:name="onedaycat.com.food.fantasy.ui.signup.SignUpActivity" />
        <activity android:name="onedaycat.com.food.fantasy.ui.forgotpassword.ForgotPasswordActivity" />
        <activity android:name="onedaycat.com.food.fantasy.ui.splashscreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>